import { createSlice, PayloadAction } from "@reduxjs/toolkit";
import { toast } from 'react-toastify';

const initialState: {}

export const exercicioSlice = createSlice({
    name: 'exercicio',
    initialState,
    reducers: {

    }
});

export const {} = exercicioSlice.actions;

export default exercicioSlice.reducer;